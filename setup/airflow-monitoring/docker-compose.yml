services:
  statsd-exporter:
    image: prom/statsd-exporter
    container_name: airflow-statsd-exporter
    command: "--statsd.listen-udp=:8125 --web.listen-address=:9102 --log.level debug --statsd.mapping-config=/home/statsd-mapping-configs.yml"
    ports:
      - 9123:9102
      - 8125:8125/udp
    volumes:
      - ./statsd-exporter/statsd.yml:/home/statsd-mapping-configs.yml
    networks:
      - airflow-untitled-project-net

  prometheus:
    image: prom/prometheus
    container_name: airflow-prometheus
    user: "0"
    ports:
      - ${PROMETHEUS_PORT}:9090
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
    networks:
      - airflow-untitled-project-net

  grafana:
    image: grafana/grafana
    container_name: airflow-grafana
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_USERNAME}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_PASSWORD}
      GF_PATHS_PROVISIONING: /grafana/provisioning
    ports:
      - ${GRAFANA_PORT}:3000
    volumes:
      - ./grafana/datasources:/grafana/datasources
      - ./grafana/dashboards:/grafana/dashboards
      - ./grafana/provisioning:/grafana/provisioning
    networks:
      - airflow-untitled-project-net

networks:
  airflow-untitled-project-net:
    external: True